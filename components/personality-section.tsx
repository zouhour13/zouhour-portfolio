import { personality } from "@/data/personality";

export function PersonalitySection() {
  return <section className="border-t border-[#edc8d5] bg-[#fff] py-24" id="beyond"><div className="mx-auto max-w-7xl px-6 sm:px-10 lg:px-16"><div className="grid gap-8 lg:grid-cols-[0.8fr_1.2fr]"><div><p className="eyebrow">Beyond the code ✦</p><h2 className="section-title mt-4">The little things that keep the work joyful.</h2></div><p className="max-w-xl self-end text-lg leading-8 text-[#6b4b59]">Engineering is one part of the picture. I also make room for strategy, color, music, movement, and noticing the details.</p></div><div className="personality-grid mt-14">{personality.map((item, index) => <article className={`personality-card personality-${item.accent}`} key={item.title}><span aria-hidden="true" className="personality-symbol">{item.symbol}</span><p className="font-mono text-xs uppercase tracking-[0.14em]">0{index + 1}</p><h3 className="mt-8 font-serif text-3xl">{item.title}</h3><p className="mt-3 max-w-xs leading-7 text-[#5c3c49]">{item.description}</p>{item.title === "Chess" && <div aria-hidden="true" className="chess-move"><span>♟</span><i /><span>♞</span></div>}</article>)}</div></div></section>;
}
