"use client";

import Link from "next/link";
import { motion, useReducedMotion } from "framer-motion";

const demoUrl = "https://text-to-sql-app-ilnwyv6bb3qqpb2w6e47bw.streamlit.app/";
const githubUrl = "https://github.com/zouhour13/text-to-sql-streamlit";

const pipeline = [
  { number: "01", title: "User question", text: "How many employees are in the company?" },
  { number: "02", title: "Schema context", text: "The known EMPLOYEE database schema is supplied to the model." },
  { number: "03", title: "LLM generation", text: "Groq-hosted GPT-OSS 120B produces a SQLite query." },
  { number: "04", title: "SQL execution", text: "The generated query runs against the local SQLite database." },
  { number: "05", title: "Result", text: "The application shows the SQL and returned data together." },
];

const implementation = [
  ["Natural-language SQL generation", "English questions are converted into SQLite queries through the LLM."],
  ["Schema-aware prompting", "The EMPLOYEE schema gives the model the context it needs to address available data."],
  ["Query execution", "Generated SQL is run against the included SQLite database."],
  ["Result presentation", "The interface displays both the generated SQL and the returned database rows."],
  ["Configuration", "Credentials use environment variables locally and Streamlit secrets in deployment."],
];

export function TextToSqlCaseStudy() {
  const reduceMotion = useReducedMotion();
  return <article className="text-sql-case"><section className="sql-hero mx-auto max-w-7xl px-6 pb-20 pt-14 sm:px-10 lg:px-16"><Link className="text-link" href="/work">← All work</Link><div className="sql-hero-grid"><motion.div initial={{ opacity: 0, y: reduceMotion ? 0 : 24 }} transition={{ duration: .6 }} whileInView={{ opacity: 1, y: 0 }}><p className="eyebrow">03 / LLM / Data application</p><h1>Text-to-SQL<br />Data Assistant</h1><p className="sql-hero-intro">An AI-powered data assistant that transforms natural-language questions into SQL queries and executes them against a structured employee database.</p><div className="sql-cta-row"><a className="sql-live-cta" href={demoUrl} rel="noreferrer" target="_blank">↗ Live Demo</a><a className="sql-github-cta" href={githubUrl} rel="noreferrer" target="_blank">⌘ GitHub</a></div></motion.div><motion.div className="sql-query-preview" initial={{ opacity: 0, scale: .96 }} transition={{ duration: .65, delay: .12 }} whileInView={{ opacity: 1, scale: 1 }}><div className="query-topline"><span>assistant.sql</span><i>Live workflow</i></div><div className="query-question">How many employees are in the company?</div><div className="query-steps"><span>natural language</span><b>→</b><span>GPT-OSS 120B</span><b>→</b><span>SQLite</span></div><pre><code><em>SELECT</em> COUNT(*) <em>FROM</em> EMPLOYEE;</code></pre><div className="query-result"><span>RESULT</span><strong>10</strong><small>employees</small></div></motion.div></div><div className="sql-meta-row"><span><b>Role</b> AI / Software Engineer</span><span><b>Type</b> LLM + Data Application</span><span><b>Status</b> Live</span><span><b>Stack</b> Python · Streamlit · LangChain · Groq · SQLite</span></div></section><section className="sql-problem"><div className="mx-auto grid max-w-7xl gap-10 px-6 py-24 sm:px-10 lg:grid-cols-[.72fr_1.28fr] lg:px-16"><div><p className="eyebrow">The problem</p><h2>Answers should not require speaking SQL.</h2></div><p>People often know the information they need from structured data, but not the SQL needed to retrieve it. This application explores how an LLM can bridge that gap: ask a question in natural language, then inspect the generated query and its result.</p></div></section><section className="sql-pipeline mx-auto max-w-7xl px-6 py-28 sm:px-10 lg:px-16"><div className="sql-section-intro"><p className="eyebrow">How it works</p><h2>From a question to a result, with the query kept visible.</h2></div><div className="pipeline-flow">{pipeline.map((step, index) => <motion.article className="pipeline-step" initial={{ opacity: 0, x: reduceMotion ? 0 : index % 2 ? 20 : -20 }} key={step.number} transition={{ duration: .5, delay: index * .05 }} viewport={{ once: true, amount: .35 }} whileInView={{ opacity: 1, x: 0 }}><span>{step.number}</span><div><h3>{step.title}</h3><p>{step.text}</p></div></motion.article>)}</div></section><section className="sql-example"><div className="mx-auto max-w-7xl px-6 py-28 sm:px-10 lg:px-16"><p className="eyebrow">A verified example</p><div className="example-sequence"><div><span>Natural language</span><p>“How many employees are in the company?”</p></div><i aria-hidden="true">↓</i><div className="example-code"><span>Generated SQL</span><pre><code>SELECT COUNT(*) FROM EMPLOYEE;</code></pre></div><i aria-hidden="true">↓</i><div className="example-answer"><span>Result</span><strong>10</strong></div></div></div></section><section className="sql-architecture mx-auto max-w-7xl px-6 py-28 sm:px-10 lg:px-16"><div className="sql-section-intro"><p className="eyebrow">Technical architecture</p><h2>A deliberately focused AI and data path.</h2></div><div className="architecture-flow"><span>Streamlit UI<small>The browser interface</small></span><b>↓</b><span>LangChain<small>Prompt and model composition</small></span><b>↓</b><span>Groq / GPT-OSS 120B<small>Hosted LLM inference</small></span><b>↓</b><span>Generated SQL<small>A SQLite query</small></span><b>↓</b><span>SQLite / company.db<small>Structured employee data</small></span><b>↓</b><span>Query results<small>Returned rows in the UI</small></span></div></section><section className="sql-implementation"><div className="mx-auto grid max-w-7xl gap-12 px-6 py-28 sm:px-10 lg:grid-cols-[.75fr_1.25fr] lg:px-16"><div><p className="eyebrow">Technical implementation</p><h2>Focused decisions, visible behavior.</h2></div><div className="implementation-rows">{implementation.map(([title, text], index) => <motion.article initial={{ opacity: 0, y: reduceMotion ? 0 : 16 }} key={title} transition={{ delay: index * .04 }} viewport={{ once: true }} whileInView={{ opacity: 1, y: 0 }}><span>{String(index + 1).padStart(2, "0")}</span><div><h3>{title}</h3><p>{text}</p></div></motion.article>)}</div></div></section><section className="sql-decisions mx-auto max-w-7xl px-6 py-28 sm:px-10 lg:px-16"><p className="eyebrow">Engineering decisions</p><div className="decision-lines"><p><strong>SQLite</strong><span>A lightweight relational database is enough to demonstrate the workflow without external infrastructure.</span></p><p><strong>Streamlit</strong><span>An efficient way to expose the AI and data workflow through an interactive browser interface.</span></p><p><strong>LangChain</strong><span>The application layer for composing prompts and integrating the chat model.</span></p><p><strong>Groq</strong><span>Hosted inference access to the selected LLM through a straightforward application integration.</span></p></div></section><section className="sql-deployment"><div className="mx-auto grid max-w-7xl gap-8 px-6 py-20 sm:px-10 lg:grid-cols-[.8fr_1.2fr] lg:px-16"><div><p className="eyebrow">Deployment</p><h2>Not only a local experiment.</h2></div><div className="deployment-detail"><p><span>Platform</span> Streamlit Community Cloud</p><p><span>Status</span> Live</p><p><span>Runtime</span> Python</p><a className="sql-live-cta" href={demoUrl} rel="noreferrer" target="_blank">Open Live Application ↗</a></div></div></section><section className="sql-source-callout mx-auto max-w-7xl px-6 pb-28 sm:px-10 lg:px-16"><div><p className="eyebrow">Explore the implementation</p><h2>See the application, LLM integration, database workflow, configuration, and deployment setup.</h2><a className="button-primary" href={githubUrl} rel="noreferrer" target="_blank">View source on GitHub ↗</a></div><Link className="text-link" href="/work/industrial-vision-inspection">Next project: Industrial Vision Inspection <span aria-hidden="true">→</span></Link></section></article>;
}
