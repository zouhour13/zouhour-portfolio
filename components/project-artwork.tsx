import type { Project } from "@/data/projects";
export function ProjectArtwork({ project, variant = 0 }: { project: Project; variant?: number }) { const motifs = ["RAG", "LLM", "VISION", "DATA"]; return <div aria-label={`${project.title} visual placeholder`} className={`project-art project-art-${variant % 4} relative aspect-[16/10] overflow-hidden p-6`} role="img"><span className="relative z-10 font-mono text-xs uppercase tracking-[0.16em]">{motifs[variant % 4]}</span><div className="absolute bottom-6 left-6 right-6 flex items-end justify-between border-t border-current pt-3"><span className="max-w-48 font-serif text-xl leading-tight">{project.category}</span><span className="font-mono text-xs">0{variant + 1}</span></div><div className="art-shape absolute" /></div>; }
