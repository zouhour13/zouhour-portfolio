"use client";
import Link from "next/link";
import { useState } from "react";
import { LanguageSelector } from "@/components/language-selector";
type Locale = "en" | "fr";
export function SiteHeader({ locale = "en" }: { locale?: Locale }) { const [open, setOpen] = useState(false); const fr = locale === "fr"; const links = fr ? [{ href: "/fr", label: "Accueil" }, { href: "#work", label: "Projets" }, { href: "#experience", label: "Parcours" }, { href: "#contact", label: "Contact" }] : [{ href: "/", label: "Home" }, { href: "/work", label: "Work" }, { href: "/experience", label: "Journey" }, { href: "/about", label: "About" }, { href: "/#contact", label: "Contact" }]; return <header className="site-header"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-5 sm:px-10 lg:px-16"><Link className="site-wordmark" href={fr ? "/fr" : "/"}>Zouhour Bellamine<span>.</span></Link><button aria-expanded={open} aria-label="Toggle navigation" className="menu-button md:hidden" onClick={() => setOpen(!open)} type="button"><span /><span /></button><nav aria-label="Primary navigation" className={`${open ? "nav-open" : ""} site-nav`}>{links.map((link) => <Link className="nav-link" href={link.href} key={link.href} onClick={() => setOpen(false)}>{link.label}</Link>)}<a className="cv-nav-link" download href="/cv/zouhour-bellamine-cv-en.pdf">{fr ? "CV" : "CV"}</a><LanguageSelector locale={locale} /></nav></div></header>; }
