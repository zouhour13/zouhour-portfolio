"use client";
import { FormEvent, useEffect, useRef, useState } from "react";
import { contact } from "@/data/profile";
export function ContactForm({ locale = "en" }: { locale?: "en" | "fr" }) { const formRef = useRef<HTMLFormElement>(null); const [visible, setVisible] = useState(false); const fr = locale === "fr"; useEffect(() => { const form = formRef.current; if (!form || window.matchMedia("(prefers-reduced-motion: reduce)").matches) return; const observer = new IntersectionObserver(([entry]) => { if (entry.isIntersecting) { setVisible(true); observer.disconnect(); } }, { threshold: .2 }); observer.observe(form); return () => observer.disconnect(); }, []); function handleSubmit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); const name = String(data.get("name") ?? ""); const email = String(data.get("email") ?? ""); const content = String(data.get("content") ?? ""); const subject = encodeURIComponent(`${fr ? "Message du portfolio de" : "Portfolio message from"} ${name}`); const body = encodeURIComponent(`Name: ${name}\nEmail: ${email}\n\n${content}`); window.location.href = `mailto:${contact.email}?subject=${subject}&body=${body}`; } return <form className={`contact-form ${visible ? "contact-form-visible" : ""}`} onSubmit={handleSubmit} ref={formRef}><label htmlFor={`name-${locale}`}>{fr ? "Nom" : "Name"}</label><input id={`name-${locale}`} name="name" required type="text" /><label htmlFor={`email-${locale}`}>{fr ? "Adresse e-mail" : "Email Address"}</label><input id={`email-${locale}`} name="email" required type="email" /><label htmlFor={`content-${locale}`}>{fr ? "Message" : "Content"}</label><textarea id={`content-${locale}`} name="content" required rows={5} /><button className="contact-submit" type="submit">{fr ? "Envoyer l'e-mail" : "Send Email"} <span aria-hidden="true">→</span></button></form>; }
